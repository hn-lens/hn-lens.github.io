# Defect register

The enumerated, persistent list of known defects. **Convergence is defined over this file**, not over
"whatever the lenses say next time" (AGENTS.md rule 7, TERMINATION).

Why it exists: every round used to produce a fresh list, so nothing was ever closed as a *set* and the
same items were re-litigated round after round. A finding that appears only because a detector got
sharper is logged here once as `pre-existing`, triaged once, and not re-argued.

**Status values:** `open` · `fixed` (with the guard that now protects it) · `accepted` (with a written
rationale — an accepted MEDIUM does not block convergence).

**A round converges when**, measured against an unchanged instrument: zero `open` BLOCKER/HIGH, zero
self-inflicted findings, and every MEDIUM either `fixed` or `accepted`.

| id | sev | status | class | summary | guard / rationale |
|----|-----|--------|-------|---------|-------------------|
| R-01 | HIGH | fixed | CODE-WRONG | Every dependency-bump PR fails the gate on `third-party notices up to date` (the notices carry a Version column a bot cannot regenerate). Now that publishing waits on the gate, a merged bump silently stops deploys instead of showing a red X. | `gen-notices.mjs` emits only the attribution-relevant facts (package set, license, homepage, upstream NOTICE, model catalog), so a version bump leaves the file identical and `--check` stays byte-exact. `noticesbumptest` asserts both halves: a version-only bump of every package passes; added / removed / relicensed / changed-NOTICE still fail. |
| R-02 | MEDIUM | fixed | CODE-WRONG | The "Not interested" fix corrected the habit COUNT but not the affinity SUM: a rejected story can still leave +2.0–2.5 of positive domain affinity, and a different story from that domain returns to rank 1. | A currently-hidden item's domain/author contribution is REPLACED by one rejection (`SIGNAL_WEIGHT.hide`, stated once), not offset by it; `perItem` moves with it so the leave-one-out agrees. `reasonstest` m6 asserts the SUM and the sibling's ranking feature for read-twice / saved-and-read (5 checks FAILED pre-fix), the opposite case (hide→UNDO restores the never-hidden totals exactly — demonstrated to fail against a build that ignores the unhide resolution), and both event orderings. `feedstabilitytest`'s "a hide WITHOUT undo stays negative" control rules out neutralising instead. |
| R-03 | MEDIUM | open | SPEC-GAP | Regime-dependent discovery failure: with a taste concentrated on few domains, saturated affinity features out-vote the content feature and no slider setting moves a position. Affinity is double-counted (own blend term + three learned features). | — |
| R-04 | MEDIUM | open | CODE-WRONG | `hover:opacity-90` composites both fill and label toward the page background, dropping primary-button labels below AA in 28 of 62 design×mode cells (worst 3.89). 9 call sites; 0/62 fail at rest. | `themecontrasttest` has no hover pass |
| R-05 | MEDIUM | open | SPEC-GAP | Searching a discussion silently discards every subtree the reader had expanded (66 visible → 43) on all three clear paths; the filter unmounts the thread and expansion lives in per-comment local state. | — |
| R-06 | MEDIUM | open | DOCS | `README.md`, `SECURITY.md` and the build skill still describe the pre-gate push→publish flow; `ci.yml` appears in no shipped doc. | — |
| R-07 | LOW | fixed | CODE-WRONG | Markdown headings are silently flattened by `mdLite` — the sibling of the bullet-marker fix; SPEC §5.2 names headings. | `mdLite` did emit `<h3>`–`<h6>`; preflight resets `h1..h6` to `font-size/font-weight: inherit`, so an `<h4>` rendered at 14px/400 above a 14px/400 paragraph with the `#` characters already consumed. Restored in `.md-body` only (never `.hn-html`), sized per level. `aiguardtest` F2 grades the RENDERED size/weight against the paragraph (FAILED pre-fix), asserts the HN-comment container is unchanged, and sweeps every other `mdLite` construct (link, quote, rule, emphasis, bold, code, ordinals) for survival. |
| R-08 | LOW | open | CODE-WRONG | An absent item on `/item/:id` renders as an outage that retries forever, because the route cannot distinguish "failed" from "does not exist" the way `getItems` now can. | — |
| R-09 | LOW | open | CODE-WRONG | The results `Clear` control also closes an unrelated open tool. | — |
| R-10 | LOW | fixed | COMMENT-WRONG | Five stale breakpoint numbers in the discussion-toolbar comments (`@xl/@lg/@md/@sm/@xs` vs the actual `@lg/@md/@sm/@xs/@3xs`). | — |
| R-11 | MEDIUM | accepted | SPEC-GAP | `releasegatecheck.mjs` asserts that specific known-dangerous regressions are absent; it does not evaluate the `if:` expression, so a semantic rewrite that keeps the substrings could pass. | Accepted: it is documented as a known-regression check, not a proof. A semantic check needs a YAML+expression evaluator, which is a larger piece of work than the risk justifies today. |
| R-12 | LOW | accepted | SPEC-GAP | Discussion toolbar leaves an empty centre below ~400px once Search folds. | Accepted in `review/SPEC.md` §11 with scope: an empty centre ABOVE ~400px, a second row, or a control folding out of priority order remains a finding. |
| R-13 | LOW | open | SPEC-GAP | The performance lens was not run in the last confirming round — a coverage gap, not a finding. | run it next round |
| R-14 | HIGH | fixed | CODE-WRONG | The convergence gate resolved its baseline commit unconditionally, but `actions/checkout` clones shallow so that commit is absent on a runner. The check errored, CI went red, and publishing was skipped silently — R-01's exact class, introduced by the fix interval that added it. Caught on the first push. | Skips under `CI`, and skips (not fails) on an unreachable baseline; all three paths verified |
| R-15 | MEDIUM | accepted | SPEC-GAP | A red CI on main for ANY reason skips the deploy with no in-repo notification. | Accepted by the maintainer: GitHub already emails on workflow failure, so a second channel would add noise, not signal. R-01 and R-14 removed the two causes that were likely to fire routinely. |
| R-16 | LOW | open | CODE-WRONG | An item whose events carried TWO different domains (its URL changed between events) charges the rejection to one of them twice and leaves the other holding credit from a rejected story. `perItem.dw` sums across domains while `rec.domain` is one last-writer-wins key. Narrow trigger; escalated from a read-only skew to a write into global totals by the affinity-floor change. | needs per-domain accrual in `perItem` |
| R-17 | LOW | open | CODE-WRONG | `mdLite` maps `####`/`#####`/`######` all to `<h6>`, so three markdown levels render identically. Models realistically emit at most `###`. | — |
| R-18 | LOW | open | CODE-WRONG | Nested bullets are flattened: `^\s*[-*]\s+` discards indentation so a sub-item becomes a sibling. Text survives, hierarchy does not. | — |
| R-19 | LOW | open | SPEC-GAP | The card TL;DR and persona summary render model output as plain text rather than through `mdLite`, so markdown reaches the reader as literal characters — visible, not deleted, but inconsistent across the four summary surfaces. | — |
| R-20 | HIGH | fixed | CODE-WRONG | `offlineuxtest` M3 flakes on the hosted runner and now BLOCKS PUBLISHING. It goes offline and reloads, so it depends on the service worker serving the shell; a controller precondition was added and it flaked again (hints=0, outage never rendered). Green locally every time. Measured ~31% false-red on main across recent history, `offlineuxtest` the most frequent. | Two independent causes, both removed. (a) The offline reload is gone: `main.tsx` exposes the shared `queryClient`, so the harness empties the in-memory cache in place and navigates in-page — no document request, so the SW cannot lose a race. Verified by running the harness with `serviceWorkers: 'block'`: M3 and the For-You isolation block pass with no SW at all. (b) `db.lists` survives a reload and the startup For-You pool caches `list:best`, so whenever that fetch settles after `clearAllData` the Best feed renders three cards offline and every M3 assertion reads as a broken outage UI — reproduced organically 1-in-6 locally, then deterministically by seeding the row (3/3 fail before, 3/3 pass after). Both caches are now emptied after the network is down, and the "nothing cached to render instead of the outage" precondition is asserted rather than assumed. Discrimination: dropping the offline copy, the Saved/Read hint, Retry, or letting `getFeedIds` swallow the error into an empty list or into stale ids each fails M3. 12/12 consecutive green, plus 5 at 6x CPU + 200ms latency, 4 at 12x + 400ms, 3 at 6x with the leftover row seeded. |
| R-24 | MEDIUM | open | CODE-WRONG | The comment count is rendered TWICE about 40px apart on a discussion — once in the story header meta row and again in the control band — and in the band it sits beside a second number (the unread count), so two unrelated figures appear adjacent. Found by a design consult, independent of the band layout work. | merge the count and catch-up into one slot |
| R-25 | MEDIUM | fixed | SPEC-GAP | Two adjacent affordances were both named "Article" with different outcomes: the story header's LINK leaves the app for the original URL; the control band's TOGGLE switches to the in-app extracted-text reader. | Maintainer decided: the link keeps "Article" (the genuine original), the in-app reader toggle becomes "Extracted article". `CommentsView.tsx` toggle `aria-label`/`title` + the tablist group label updated. Guarded by `discussionviewtest` [C2] (external link named "Article", toggle named "Extracted article", no two affordances share one article-name; demonstrated FAIL pre-fix — both read "Article"). Three existing guards hard-coded the old name and were updated in the same interval: `wrapqualitytest` (3 selector refs + the `Discussion|Article` name-sequence + its check text) and `focustest` (3 `getByRole('tab')` name refs). `articlelinktest`/`modeltest` reference the separate "Article text" card link and were correctly left alone.
| R-22 | MEDIUM | fixed | CODE-WRONG | The discussion control band is TWO rows at every width: the Discussion/Article toggle owns a private row that is 21.5-62.5% full and never degrades, costing 45-60px and pushing the first comment below the fold on a 360px-tall touch viewport. Violates SPEC 7.1. Developer-reported, then independently rediscovered by the UI/UX lens. | The toggle moved INTO `.disc-tb-bar` and joined the fold ladder; the bar now renders in both the discussion and article views so the toggle keeps its position across a view switch. It is drawn as two icon segments (`aria-label` carries the name) because the words do not fit the row's budget at any shipped column width — the reading column caps at 46rem and the labelled toggle needs 752-809px alongside the sort control, the search filler and the tools, so keeping them would have meant permanently folding Summary/Ask, which the AI-discoverability rule outranks. `wrapqualitytest` measures the BAND (toggle + bar, bucketed by centre-y, with `display:contents` wrappers flattened to the real flex items) across 116 cells per pointer type: width 320-1440 in 40px steps x {reader, terminal} x {default, large text}, plus the same matrix under a coarse pointer and again at max content. Discrimination: 8 of its checks fail on the pre-fix build, 116/116 cells reporting 2 rows at 22%/96% fill. |
| R-23 | MEDIUM | fixed | CODE-WRONG | Under the Large reading-text setting the toolbar itself wraps, orphaning the catch-up button alone on a second line at 15-39% fill in all 31 designs, non-monotonically as the window widens; also at default text with a 4-digit count. | Root cause: the `/tb` fold thresholds were FIXED PIXELS while every control in the row is rem-sized, so the text-size axis grew the content without moving the fold points. They are now rem, which tracks the live root font-size (verified: a 600px container matches `600px` at both text sizes but stops matching `37.5rem` once the root is 18px). The catch-up gained a fold path — it sheds its word, then folds into the "…" — and its count is capped at `999+` so a data-driven label cannot grow without bound. Thresholds were re-derived against the container query's real reference, the wrapper's BORDER box, not the bar's content box. `wrapqualitytest` now sweeps `textSize` md and lg. Discrimination on the pre-fix build, toolbar alone: reader/lg 2 rows at 640/440/320 and terminal/lg at 720/680/640/520/440/320, second row 15-37% full, row sequence 1440->320 `11111111111111111122211212112` (non-monotone); md clean in both themes, which is the signature of the text-size axis. Post-fix: 1 row at all 29 widths in all four theme x text-size combinations. |
| R-21 | LOW | open | SPEC-GAP | `offlineuxtest`'s story-outage and User-profile sections still need the service worker, not for a navigation but to serve the lazy `/item` and `/user` route chunks offline (measured: those 3 checks are the only ones that fail under `serviceWorkers: 'block'`). That is the same capability `offlinetest` M-A asserts deliberately, so it is a real dependency on a guarded feature rather than a race — but it is a second way this harness can go red for a reason other than the behaviour it names. | would need the two chunks warmed during an online window first, as `offlinetest` already does for `/user`; not done here to keep the interval small |
| R-29 | LOW | open | CODE-WRONG | Sibling of R-23 on the feed surface: `index.css` forces `.sc-actions { flex-wrap: nowrap }` for the `grid` and `masonry` layouts from a FIXED-PX viewport media query (`min-width: 1100px`) while the action row is rem-sized, so at the Large reading-text setting the row overflows its own box by 4px in every card at 1100/1200/1440px. Default text is clean in both layouts, which is the same text-axis signature as R-23. Measured identically on the pre-fix and post-fix builds, so it is pre-existing, not self-inflicted. | Not fixed here: it is a different surface with no lens report behind it, and folding it into this interval would mix an unrelated change into a band fix (AGENTS.md rules 7b and 8). Needs its own interval with a guard and an opposite-case twin. |
| R-30 | LOW | open | CODE-WRONG | The 2-segment fallback of the comment Sort control offers only `Newest`/`Replies`, so when the active sort is `Default` or `Oldest` NEITHER segment is pressed: the control shows no value and no selection, using the same bordered-track chrome that means "this one is selected" at wider widths. It is also information-non-monotonic — the NARROWER single `⇅ Default` toggle states the current sort while the wider 2-segment form does not. Pre-existing; independently found by a read-only visual review of the band. | Not fixed here: it is an information-architecture defect in the sort control rather than a row-shape defect, and collapsing or re-labelling that rung changes the fold ladder this interval just re-derived. Needs its own interval. |
| R-31 | LOW | open | CODE-WRONG | Three max-content cells sit within 1-9px of wrapping the discussion band (`bigcount/terminal/md` at 760, 720 and 520): a 4-digit comment count plus a 4-digit unread count in the widest theme. They fit, but a font-metric difference between machines can flip them — which is exactly how a green local gate produced a red hosted one. A container query measures the CONTAINER, so no threshold can fold the tools for the monospace theme alone; it needs narrower tools (icons at the widest) or a theme-aware budget. | `wrapqualitytest` now measures headroom and requires >=12px on the realistic sweeps; the max-content sweep is exempted and tracked here |
| R-26 | LOW | open | CODE-WRONG | The shared `IconButton` (`ui/primitives.tsx`) renders no border and no fill — only a hover background — so the band's "…" overflow is the one control in a row of `--edge`-bordered controls with no visible boundary (WCAG 1.4.11), and at the narrowest widths it is the sole route to sort, search, Summary, Ask and the catch-up. It also makes the row read as right-shy: the button's box sits 8-9px off the right inset, but its ink sits 21-24px off. Pre-existing. | Not fixed here: `IconButton` is app-wide and giving it a boundary is a visual change across every surface that uses it, which needs its own sweep. |
| R-27 | LOW | open | CODE-WRONG | Two cosmetic inconsistencies inside the band, both pre-existing. (a) Control heights differ by up to 8px in one row (sort track / search / view switch / Summary-Ask / catch-up), so the row has a stepped silhouette — everything is correctly centred, so this is sizing, not alignment. (b) `.seg-act` hard-codes `border-radius: 9px` instead of the `--radius-lg` token every sibling uses, so the square-cornered designs (terminal, swiss, brutalist) render the Summary/Ask pair as the only rounded object in a square row. | Not fixed here: (b) is a one-line token swap but it changes the rendering of a control in several designs with no contrast/visual guard covering it, and (a) needs a decision about one canonical control height. |
| R-28 | LOW | open | CODE-WRONG | Once the catch-up button sheds its word below 30.5rem it renders as `↓ N`, and on a first visit every comment is new, so the comment count and the catch-up count are the same number twice in one row, distinguished only by the arrow glyph and the accent tint. The accessible name still says "Jump to the next of N new comments". Introduced by the catch-up's new fold path in this interval. | Accepted for now rather than fixed: restoring the word costs ~25px at exactly the widths where the row has none, and the icon plus the accent tint plus the accessible name do carry the distinction. Revisit if a lens reports a reader actually confusing them. |
| R-32 | HIGH | fixed | CODE-WRONG | The story-card popover is placed once at open time and never re-placed. With the anchor low in the viewport the clamp flips the menu ABOVE it, and the next downward scroll walks it off the top of the screen: at 360x640 a +350px scroll left the menu at y=-69, overlapping the pinned header by 57px and stealing the clicks of four top-nav controls. The discussion toolbar shares the hook but its anchor is sticky, so it cannot drift. | REOPENED then closed properly. The first fix re-placed on scroll but left the placement RULE wrong, so the drift persisted from a scroll of 350px at 360x640; the guard passed because it was calibrated to a geometry where the defect does not appear. `cardteachtest` now sweeps two phone geometries x four scroll depths; demonstrated FAIL pre-fix on a rebuilt bundle. |
| R-33 | MEDIUM | fixed | CODE-WRONG | One Escape dismissed TWO layers: closing the overflow menu also cleared the in-thread filter underneath it. The second dismissal was not our code at all — Chrome natively clears a focused `input[type=search]` on Escape — so the handler needed `preventDefault()` plus a precedence guard that stops the tool-level handler running while the menu is the topmost layer. | `discussionviewtest` F9, demonstrated FAIL pre-fix |
| R-34 | MEDIUM | fixed | CODE-WRONG | The discussion view is reused across ids, so a filter or an open tool followed the reader into a DIFFERENT discussion and rendered "0 matches" over a thread that has comments. | `discussionviewtest` F10, demonstrated FAIL pre-fix; `gisttest` corrected — it had been asserting the leaked state |
| R-35 | LOW | fixed | CODE-WRONG | Saving a story offered an Undo but unsaving did not, so the destructive half of the pair was the unrecoverable one. | covered by the toast assertions in `feedstabilitytest` |
| R-36 | LOW | fixed | CODE-WRONG | In the article view the control band holds only the view toggle, but the bar was `w-full`, so it framed a mostly-empty rule across the column. | band now hugs its content; `wrapqualitytest` measures row fill |
| R-37 | BLOCKER | fixed | CODE-WRONG | Opening a story menu raised its CARD to the same stacking level as the pinned header, and the card comes later in the document, so the card painted over the entire bar: all 5 header controls became unhittable over ~160px of scrolling and a tap on the search field landed on the story headline beneath it. The menu box itself overlapped by 0px, so this is a different defect from R-32 and was invisible to the guard written for it. | `cardteachtest` now asks whether each header control receives its OWN hit rather than whether the menu is the thing covering it; demonstrated FAIL pre-fix (5 stolen, `stolenBy` the card's stretched title link) |
| R-38 | HIGH | fixed | CODE-WRONG | Self-inflicted by R-32's first fix: re-placing on scroll rebuilt the height cap from scratch, and dropping the cap re-lays the element out at full height, which makes the browser clamp scrollTop to 0. A capped menu could not be scrolled at all, so its last entries were unreachable on short viewports — and on a phone that menu is the ONLY per-story control, every other one having folded into it by design. | `cardteachtest` capped/uncapped pair, with an assertion that at least one geometry actually capped (the fixture menu fits outright at the taller sizes, so the capped branch would otherwise never execute) |
| R-39 | HIGH | fixed | CODE-WRONG | The header search placeholder was cut mid-word below 375px (145px of text into 86px of field), reading as a typo rather than as truncation, because an input cannot ellipsise its own placeholder. | `headerfittest`, sweeping 9 widths x 2 reading sizes; demonstrated FAIL pre-fix |
| R-40 | HIGH | fixed | CODE-WRONG | The theme and layout menus were trimmed by a fixed max-width even at 1920, where roughly 800px of the bar was empty, so neither showed the setting it was on. | `headerfittest` asserts the selected value fits at >=1024px at the default reading size; demonstrated FAIL pre-fix |
| R-41 | MEDIUM | open | SPEC-GAP | The app has five different page frame widths (header 6xl, feeds 6xl, settings 5xl, item/user/saved 3xl), so on every page except the feeds the header does not share a left edge with the content: measured 64px of offset on settings and 192px on saved. Whether the reading column should left-align to the frame or stay centred is a design decision, not a defect with one right answer. | Not fixed here: needs a mockup of each option for the maintainer to choose between, per the project's rule against picking a visual direction unilaterally |
| R-42 | MEDIUM | open | SPEC-GAP | The saved-page empty state is a fixed 212px block pinned at y=117 at every size, leaving 49-70% of the frame as flat colour; nothing in it scales with the frame. | — |
| R-43 | MEDIUM | open | CODE-WRONG | Touch targets below the 44px minimum on phones: card action controls measure 32px at 320/360 and header icon buttons 36px at every size. Raising them risks the discussion band's fold thresholds, which are tuned to control widths, so it needs its own interval with the band's wrap guard watched. | — |
| R-44 | MEDIUM | open | SPEC-GAP | The "Why #N?" control renders the application's own brand mark as its icon; an independent reviewer holding only screenshots could not distinguish it from the header logo. | — |
| R-45 | LOW | open | CODE-WRONG | `convergencecheck` enumerates changed files with `git diff`, which does not see UNTRACKED files, so a brand-new instrument file added alongside a src change passes the instrument-freeze check. Observed while adding `review/base/device-matrix.md`. | — |
| R-46 | LOW | open | CODE-WRONG | A trimmed menu scroll-chains to the page on an overshooting wheel flick and closes; touch never chains. Needs `overscroll-behavior: contain`. | — |
| R-47 | HIGH | open | CODE-WRONG | On a LANDSCAPE phone (740x360, 640x360) the discussion toolbar with its tray open is taller than the space a menu needs, so clearing it and showing the menu cannot both be satisfied: the menu overlaps the toolbar and one tray control loses its tap. Portrait is clean (0 stolen at 320/360/375/390) and the menu is on-screen at every size. | Not fixed here: at 360px of height there is genuinely no placement that satisfies both constraints, so this needs a different presentation (a sheet, or closing the tray when the menu opens) rather than better arithmetic -- a design decision, not a bug with one right answer. |
| R-48 | BLOCKER | fixed | CODE-WRONG | Self-inflicted by R-37's fix: the placement band was built from the bar's bottom and never clamped to the band's own bottom, so a tall toolbar produced an empty or inverted band. The menu collapsed to a max-height of 0 (no item tappable) and at 320x640 was parked entirely off-screen at y=675 in a 640px viewport. | The band now always keeps a usable strip; verified on-screen with 0 stolen controls at 320/360 portrait |
| R-49 | MEDIUM | fixed | CODE-WRONG | The discussion toolbar's own search placeholder was cut mid-word (needed 174.5px into 56px) — the same defect already fixed in the top bar, on a field the guard did not look at. | `headerfittest` now sweeps a live discussion as well as the feed and measures EVERY placeholder in a pinned bar, asserting once per run that the toolbar field was actually seen |
| R-50 | ACCEPTED | closed | SPEC | Page content and the top bar do not share a left edge on Settings (~64px) or reading pages (~192px). Maintainer deliberated and accepted: it does not bother them, and the achievable alignment would still leave text baselines ~8px apart. Recorded in SPEC 11. | Not a defect. Reviewers instructed not to report it again. |
| R-51 | ACCEPTED | closed | SPEC | The "Why #N?" control uses the app's brand mark as its icon. Maintainer's own design choice. Independently reported by three separate reviewers before being closed. Recorded in SPEC 11. | Not a defect. Reviewers instructed not to report it again. |
| R-52 | ACCEPTED | closed | SPEC | On a screen under ~500px tall the discussion "..." menu opens as a centred dialog over a dimmed page rather than from its trigger. Maintainer accepted. Still in scope if it is unreachable, clipped, offers no way out, or appears on a screen that is not short. | SPEC 11 |
| R-53 | ACCEPTED | closed | SPEC | Tap targets are 36px rather than 44px on screens under ~500px tall. Maintainer's decision, taken to buy back screen space; comfortably clears the 24px minimum. | `contentbudgettest` thresholds assume it; SPEC 11 states the exception, and under 36px anywhere remains a defect |
| R-54 | HIGH | fixed | CODE-WRONG | The toolbar had no declared priority between its four constraints (no overflow, tap size, one row, no gap), so each fix re-litigated the last and the failing-cell count oscillated 12 -> 7 -> 2 -> 12 across attempts. The maintainer set the order: sort yields LAST, everything else first, and the sort segments take the width the filter leaves so its departure is not a hole. | `wrapqualitytest` fold-order assertion rewritten to that priority; demonstrated FAIL on the previous order |
| R-55 | HIGH | fixed | CODE-WRONG | Self-inflicted: a guard asserted the sliver filter "ellipsises rather than hard-cuts" by reading the computed `text-overflow` property, while the field actually rendered a letter and half an ellipsis dot. Green for the wrong reason, by the author of the guard. | The field now draws no placeholder at that width; the guard checks whether the placeholder is PAINTED, not whether a CSS property is set |
| R-56 | MEDIUM | open | CODE-WRONG | The discussion tools panel draws only 45-65% of its entries at 320/360/375 with no scroll cue; at 320 it ends on the heading `SORT` with none of its options beneath. Reachable by scrolling, so not a BLOCKER, but nothing indicates there is more. | Not yet gated |
| R-57 | MEDIUM | open | CODE-WRONG | Two harness flakes observed in one session: `audit` ("Read tab" empty, did not recur on rerun of identical code) and `wrapqualitytest` (failed inside the gate, passes standalone). Flaky gates destroy the only convergence signal there is. | Not yet gated |
| R-58 | LOW | open | SPEC-GAP | Nothing scales past 1280px: 42-62% of a 1920 screen is empty margin and the layout is pixel-identical to 1280. Reported independently by every page reviewer. | Deferred: an OBSERVATION under SPEC 9a, not a criterion violation |
| R-59 | MEDIUM | fixed | CODE-WRONG | The feed's meta row wrapped to TWO rows rather than shrinking, stranding "Updated just now" alone on a line at 30-38% fill with the Top-comments switch and Refresh orphaned beneath it: 320/360 at the default reading size and 320-390 at Large. Page overflow is 0 throughout, so no overflow guard could see it. Same class as R-23 on the discussion band, on the surface that is the app's landing page. Reported by a lens, dismissed by a prior measurement as a false positive, and confirmed real on re-measurement. | Root cause was stated in the row's own comment: the control group could not shrink, so the browser wrapped instead. The status is now the flexible filler (`flex-1 min-w-0 truncate`), the buttons are `shrink-0`, and the group's label folds below 26rem — which is what makes one row safe, since removing the wrap alone re-creates the page overflow the wrap was added to prevent. `feedrowtest` sweeps 7 widths x 2 text sizes x 2 feeds; demonstrated FAIL pre-fix on a rebuilt bundle (16 of 28 cells, 2 rows), PASS after, with the status still painted at full text at the tightest cell (123px at lg/320). |
| R-60 | HIGH | fixed | CODE-WRONG | The meta row's two controls were 24px tall at the default reading size and 27px at Large — under the 44px floor and under the relaxed 36px floor too — on every feed at every phone width. `.feed-meta` had no rule anywhere in `index.css`, so it was the one control family absent from every `@media (pointer: coarse)` block. Pre-existing; it had no hook class to be listed by until this interval added one. | Added to the existing criterion-6 block beside `.feed-tabs button` and `header button`, with the `html[data-short]` 36px variant. `feedrowtest` asserts >=44px per control at a 780px-tall viewport and treats an unmeasurable height as a failure rather than letting NaN pass a `<`; demonstrated FAIL pre-fix on all 28 cells at 24/27px, PASS after at 44/49.5px. |
| R-61 | MEDIUM | fixed | CODE-WRONG | SELF-INFLICTED by R-59's first attempt. Folding the "not shown in Compact layout" note away below 32rem left a switch that is disabled yet drawn ON with no visible reason, on exactly the phone widths where `compact` is the default layout (terminal and cyberpunk designs). Restoring the note inside the row then traded that for the mirror-image defect: the row overflowed its own box by 10-32px and the PAGE by 18px at 320. | Resolved by taking the explanation out of the control row entirely — it is prose, not a control, and it now renders on its own line, so it is visible at every width and competes for no horizontal room. `feedrowtest` gained a `compact` phase measuring the row's OWN box as well as the page; BOTH broken states were demonstrated to turn it red (4 failures on the hidden note, 3 on the in-row overflow reproducing the exact 10/32/18px figures) before the final state passed. |
| R-62 | BLOCKER | open | CODE-WRONG | A story's overflow menu is painted UNDERNEATH the sticky feed-tab strip at `scrollY = 0` — the state the reader arrives in. Reproduced on the real For You feed in the tile layouts at Large text: 375x844/grid, 360x780/grid and 390x844/bento all place the menu at y=119 while the strip occupies [82,150], a 30px overlap. Originally observed as a STOLEN TAP (the first entry took the "New" tab's tap, so it changed feed instead of opening the story). | Root cause, confirmed by reading `usePopoverClamp.ts`: `pinnedHeaderBottom()` only counts a bar it judges CURRENTLY STUCK (`r.top <= offset + 0.5`), but a sticky bar resting at its natural offset at scroll 0 is not stuck and is skipped — while still painting over a lower-z menu. The test asks "is this bar stuck?" when the question is "will it paint over the menu?". THREE things learned here that the fixing interval needs. (1) The stolen-tap symptom is a KNIFE EDGE, and this interval's unrelated tap-target change masked it: making the meta row 20px taller moved the menu 119px -> 136px, so the first item's centre cleared the strip by 3px and `stolen` read 0 while the menu still painted 22px under the bar. Gate on OVERLAP, not on a stolen centre. (2) `cardteachtest` CANNOT host this: its fixture places the menu at y=275 whatever the viewport or layout, so a phase added there passed pre-fix and was removed rather than shipped as false assurance. It needs the real feed. (3) The one-line clamp fix (drop the `stuck` gate) removes the overlap — menu moves to y=156, clear of the strip — but is NOT sufficient on its own: it lowers the menu enough that its last entry is then taken by a sibling card in the same three cells, so the naive fix trades the bar overlap for the R-37 class. It was reverted rather than shipped half-understood. Repro on disk: `node scripts/popoverbandtest.mjs` (BASE=<preview>), currently 3 cells red with 2 opposite-case cells green; wire it into the gate as part of the fix.
| R-63 | HIGH | open | CODE-WRONG | The tab-strip edge fade washes out a tab that is FULLY rendered on screen. At 360 default text the "Best" pill spans [272.6, 349.9] and ends 10px clear of the viewport edge, yet 33.9px of its 77.3px — 44%, including its label — lies under the `from-bg` gradient; at 320 Large it is "New", 30.9px of 87.4px. An independent visual pass measured the washed glyphs at 1.4-2.7:1 against the 4.5:1 criterion 7 requires. The affordance lies: it makes a complete, tappable tab look scrolled-off. | Not fixed here. ORIGIN CORRECTED: first recorded as self-inflicted by `7badc2e`, which is WRONG. That commit moved the fade's outer end from 348 to 360 and lengthened the ramp from 32px to 44px, but left its INNER edge at x=316 unchanged — so the overlap with "Best" went 32px -> 33.9px while the gradient got gentler over the label (alpha at x=340 falls from ~0.75 to ~0.55). The previous interval made this slightly BETTER; the defect predates it. Two probes returned vacuous results before this measurement stood up: the fade divs are SIBLINGS of `.feed-tabs` so the obvious selector root finds none, and `-mx-3 px-3` cancel so a "wider than the viewport" correction was also wrong. A fix must hold both ends: narrow enough not to wash a fully-visible tab, wide enough to still cover the seam `7badc2e` closed.
| R-64 | MEDIUM | open | CODE-WRONG | In `compact` the Top-comments switch is `disabled` yet drawn in its ON state. R-61 made the REASON visible again, but the control still asserts a setting that has no effect there, which is a state shown incorrectly rather than merely unexplained. | Not fixed: changing how an inoperative control renders its state is a control-design decision, not a row-shape one, and it belongs with a decision about whether the control should appear in that layout at all. |
| R-65 | MEDIUM | open | CODE-WRONG | More instances of the R-43 tap-target class, reported by an independent interaction pass and NOT yet re-verified here: the hidden-stub "Restore" control at 43.9x16px, the card preview's "Read N comments" at 127.7x17.25px (while its sibling `.sc-comments` to the same destination is correctly 44px), `.sc-actions` icon buttons at 32px on <=360/default, and the header brand link at 24px wide. | Recorded for the interval that closes R-43, where the whole class should be swept at once rather than one control at a time. |
| R-66 | LOW | open | CODE-WRONG | The story card's action row breaks into two lines at 320 with Large reading text, pushing the overflow control onto its own line and leaving roughly 86px of dead space in a 261px card — the same width renders one clean row at the default text size. Reported by an independent visual pass. Sibling of R-29 (`.sc-actions` governed by fixed-px media queries while its content is rem-sized), and further evidence that the whole feed surface still carries the text-axis defect the discussion band had. | Fold into the R-29 interval. |
